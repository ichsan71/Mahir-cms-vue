/**
 * Kunci permission fitur Persetujuan Cuti (Leave Approval) — tiap nilai adalah
 * nama operasi GraphQL yang di-gate backend (cocok dengan `user.userPermissions`).
 */
export const PERM = {
  LIST: "listLeaveApproval",
};
