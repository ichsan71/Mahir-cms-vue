/**
 * Kunci permission fitur Saldo Cuti (Leave Balance) — tiap nilai adalah nama
 * operasi GraphQL yang di-gate backend (cocok dengan `user.userPermissions`).
 */
export const PERM = {
  LIST: "listLeaveBalance",
};
