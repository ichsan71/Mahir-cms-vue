/**
 * Kunci permission fitur Mutasi Saldo Cuti (Leave Balance Transaction) — tiap
 * nilai adalah nama operasi GraphQL yang di-gate backend.
 */
export const PERM = {
  LIST: "listLeaveBalanceTransaction",
};
