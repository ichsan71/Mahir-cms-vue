/**
 * Kunci permission fitur Laporan — tiap nilai adalah nama operasi GraphQL yang
 * di-gate backend (cocok dengan `user.userPermissions`).
 */
export const PERM = {
  LIST: "reports",
  AGGREGATE: "agregat",
};
